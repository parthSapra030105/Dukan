# Bolna tool configs — final JSON for each

For each tool, **paste the curl** into Bolna's "Generate from cURL" modal,
then **replace the entire generated JSON** with the corresponding block below,
then click **Save**.

The 5 tools you've done or are doing:
1. ~~lookup_customer~~ ✓ saved
2. ~~catalog_search~~ ✓ saved
3. ~~validate_address~~ ✓ saved
4. place_order — paste #4 below
5. escalate_to_human — paste curl + then #5 below

Delete this file when you're done with all 5.

---

## 4. place_order — REPLACE the whole JSON with this

```json
{
  "name": "place_order",
  "description": "Place the final order after the customer explicitly confirms. items must be a JSON-string array of objects like '[{\"sku\":\"atta-aashirvaad-5kg\",\"name\":\"Aashirvaad Atta 5kg\",\"qty\":1,\"price_at_order_paise\":28500,\"unit\":\"5kg\"}]' — serialise as a string, do not pass a real array. total_paise is the integer paise total (multiply qty by price_at_order_paise per item, then sum). Use customer_id if lookup_customer returned one; otherwise pass customer_phone (the caller's number from user_data). Call exactly once, only after the customer says yes to your read-back. Returns order_id and SMS confirmation.",
  "pre_call_message": "Order place kar rahi hu, ek minute…",
  "parameters": {
    "type": "object",
    "properties": {
      "call_id":          { "type": "string" },
      "customer_id":      { "type": "string" },
      "customer_phone":   { "type": "string" },
      "items":            { "type": "string" },
      "delivery_address": { "type": "string" },
      "delivery_slot":    { "type": "string" },
      "total_paise":      { "type": "string" },
      "language":         { "type": "string" }
    },
    "required": [
      "call_id",
      "items",
      "delivery_address",
      "delivery_slot",
      "total_paise",
      "language"
    ]
  },
  "key": "custom_task",
  "value": {
    "method": "POST",
    "param": {
      "call_id":          "%(call_id)s",
      "customer_id":      "%(customer_id)s",
      "customer_phone":   "%(customer_phone)s",
      "items":            "%(items)s",
      "delivery_address": "%(delivery_address)s",
      "delivery_slot":    "%(delivery_slot)s",
      "total_paise":      "%(total_paise)s",
      "language":         "%(language)s"
    },
    "url": "https://dukan-bice.vercel.app/api/agent/tools/place-order",
    "api_token": null,
    "headers": {
      "Content-Type": "application/json"
    }
  }
}
```

---

## 5. escalate_to_human — first paste this curl in the "Generate from cURL" modal:

```bash
curl -X POST https://dukan-bice.vercel.app/api/agent/tools/escalate \
  -H "Content-Type: application/json" \
  -d '{"call_id":"%(call_id)s","reason":"%(reason)s","transcript_so_far":"%(transcript_so_far)s"}'
```

Then **REPLACE the whole generated JSON with this:**

```json
{
  "name": "escalate_to_human",
  "description": "Hand off the call to a human operator. Call IMMEDIATELY when any escalation trigger fires: payment dispute, return/refund request, complaint, off-menu request, bulk order (>50 items or >₹10,000), three-strike misunderstanding, or explicit customer request for a human. Pass call_id from user_data, a short slug reason (e.g. 'payment_dispute', 'bulk_order', 'complaint', 'off_menu', 'human_request', 'misunderstanding'), and a one-paragraph summary of the conversation so far as transcript_so_far. After calling, say 'Ek minute, main aapko humare team member se baat karwaati hu' and remain quiet — a human will pick up.",
  "pre_call_message": "Ek minute, main connect karwa rahi hu…",
  "parameters": {
    "type": "object",
    "properties": {
      "call_id":           { "type": "string" },
      "reason":            { "type": "string" },
      "transcript_so_far": { "type": "string" }
    },
    "required": [
      "call_id",
      "reason",
      "transcript_so_far"
    ]
  },
  "key": "custom_task",
  "value": {
    "method": "POST",
    "param": {
      "call_id":           "%(call_id)s",
      "reason":            "%(reason)s",
      "transcript_so_far": "%(transcript_so_far)s"
    },
    "url": "https://dukan-bice.vercel.app/api/agent/tools/escalate",
    "api_token": null,
    "headers": {
      "Content-Type": "application/json"
    }
  }
}
```

---

## After all 5 are saved

1. Save the agent (click the "Save agent" button top-right)
2. Verify your phone in Bolna dashboard if not done (OTP)
3. **In Vercel env vars** — set `BOLNA_WEBHOOK_ALLOWED_IPS=` (empty string) temporarily so we can see what IPs Bolna uses for tool calls. Redeploy.
4. Click "Get call from agent" — your phone should ring
5. Test: `"Mujhe ek kilo aata aur ek paket dahi chahiye, FC Road pe shaam ko deliver kar do"`
6. Open Vercel logs in parallel — expect to see POSTs to `/api/agent/tools/lookup-customer`, `/api/agent/tools/catalog-search`, `/api/agent/tools/validate-address`, `/api/agent/tools/place-order`

Tell Claude what happened, paste any error from Vercel logs.

**Then delete this file:** `rm .tmp-bolna-tools.md`
